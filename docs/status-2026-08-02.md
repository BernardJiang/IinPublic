# IinPublic — Project Status Snapshot (2026-08-02)

Sections A through Y in `docs/TODO.md` are all complete except four small,
deliberately-open items. `docs/TODO.md` was condensed this session from 1,983
to 656 lines — every fully-shipped section is now a short "Complete" callout
pointing to `docs/completed.md` for the full historical trail; nothing was
deleted, just relocated.

## What's fully shipped

- **A–T**: AppBar/notifications/conversation entry, layout catalog (G/I/J,
  minus nightly cross-platform specs blocked on CI infra), TechSupport
  built-in presence (K1–K6, L1/L2 counters+retention), GUI graph-traversal +
  tab compaction (Q/M), DM traceback (N/O/P), fast-first-render for
  Contacts/Talks/Me (R1–R3), merkle-checkpoint pruning implementation (S),
  chatroom broadcast isolation fix (T).
- **U**: broadcast to a contact group with deferred/offline delivery.
- **V**: Auto Linear Capture (DM shorthand → flow talk), two-author credit
  model, edit-mints-new-id policy.
- **W**: unified ledger-based send-suppression (`TALK_SENT`/`sent` map, one
  `shouldSuppress()` used by room broadcast, group broadcast, and "Send My
  Talks"), plus this session's two correctness fixes — survey completeness
  and the dedicated-Ignore-withholds-response semantics.
- **X**: `Talk.authorLocation` now blurred by default (closed a real SRS
  `FR-CR-8`/`NFR-S-1` violation).
- **Y**: copy-then-edit authorship transfer fixed; incoming-talk-cluster
  retention added.

## Genuinely open (left visible on purpose)

- **K7** — TechSupport answer delegation to a co-operator pool. Explicitly
  deferred by Bernard; needs an `[Opus]` design note before any
  implementation.
- **S** — two items: the production retention-budget formula (decided, not
  yet implemented) and an unreliable message-pruning bug (root cause not yet
  found).
- **R4/R5** — chatroom member list / conversations list pagination.
  Low-priority by design, not real gaps.
- **W** — whether `sendBulkTalk`/`BulkSendJob` should be deleted as dead code
  or finished as a real feature.
- **V** — one narrow call: whether a metadata-only talk edit (title/tags)
  should reassign `authorId`/`createdAt`/`authorLocation` to the editor.
- A noted (not scheduled) doc-hygiene gap: CLAUDE.md's "Route modules"/"Talk
  delivery flow" prose still names server files/paths removed in the P2P
  migration — flagged in Y2's `docs/completed.md` entry as its own future
  pass.

## Repo state

Both `docs/TODO.md` and `docs/completed.md` are committed (`834286ac`). Test
suite was green (95/95 Jest) throughout the session's code changes; the docs
cleanup pass itself touched no code.
