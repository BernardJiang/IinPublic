# Archive

This directory holds non-current repository material that is still worth keeping for reference.

Current contents:

- `example/` - older example app code that is not part of the main product path
- `legacy-root/` - legacy top-level files kept for historical reference during cleanup

Current source-of-truth docs live under `docs/current/`. Historical documentation imports live
under `docs/archive/`.
