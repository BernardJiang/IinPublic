# Contacts Filter by Name

Bootstrap two users (Alice, Bobby). Both automatically get a TechSupport conversation.
Open Alice's Contacts tab, type partial stage name into filter input (#contacts-filter-name),
verify matching contacts stay visible while others hide. Type garbage to empty the list,
then clear to restore all visible contacts.
