# Mobile chatroom hierarchy navigation (390x844 viewport)

On a phone-sized viewport (390×844), the user navigates the chatroom hierarchy from Global through Europe → UK → London, verifying that each room title updates in the status bar, navigation elements remain clickable, and the bottom nav bar stays within bounds without overlapping the content.
