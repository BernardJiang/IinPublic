# 2026-08-22

- Continued identity architecture WP3 after completing the Settings shell and WP2 design gate.
- Versioned pairing codes now contain a schema version, random request ID, SEA public identity,
  one-time secret, and five-minute expiry. Unsupported/legacy formats fail closed.
- Added persisted expiring outgoing operations, consumed-code replay tracking, a public signed
  request inbox that never publishes the pairing secret, retry-safe partial writes, and explicit
  reciprocal approval.
- Local linked rows are treated as candidates only. Each page open refreshes them from signed
  attestation/revocation graph state before displaying `Linked`.
- Added a two-installation browser E2E (`stage2/73`) proving one-sided waiting state, explicit
  mutual approval, both-signature verification, replay rejection, and revocation convergence.
- Replaced the QR placeholder with a real `qrcode` canvas. Added capability-gated camera scanning
  through `BarcodeDetector`; typed input remains available and scanning never auto-signs.
- Added public-correlation warnings, no-transfer/no-recovery copy, and peer fingerprint previews
  before both approval actions.
- Relevant typecheck, lint, 22 focused unit tests, stage1/71, stage2/73, and production web build
  passed. `npm audit --omit=dev` still reports 21 pre-existing production dependency findings;
  the newly added `qrcode` package was not identified in the report.
- Committed the WP0–WP3 slice as `9442cdc9` (`feat(identity): implement local identity and mutual
  linking`), excluding the user-owned `AGENTS.md` worktree change.
- Started WP4: unlink is now local-first and queues a signed versioned revocation before network
  publication. Link trust becomes false immediately; failed graph writes remain `Revocation
  pending` and retry on startup/Settings refresh. Verified publication becomes `Removed`, malformed
  events become `Invalid`, and newer post-revocation attestations become `Conflicted`.
- Extended stage2/73 so the remote device is offline during revocation, later reconnects and
  converges to Removed, then follows the device-sale path: a Removed historical row is not offered
  as a sync target, erase clears the retired installation, and fresh boot creates unrelated keys.
- Committed WP4 as `844d4ffa` (`feat(identity): add durable link revocation`), again excluding the
  user-owned `AGENTS.md` change.
- Audited the remaining architecture plan: WP0/WP1/WP3/WP4 are complete; WP2 is the next dependency
  gate and WP5–WP10 remain, so work did not skip forward to WP5.
- Approved WP2 for staged implementation with scrypt rather than Argon2id because the candidate
  JavaScript Argon2 implementation was outside its independent audit scope. Pinned
  `@noble/hashes@1.8.0` and selected OWASP's `N=2^16, r=8, p=2` profile (~64 MiB); local derivation
  benchmark was 181–185 ms on the development Apple Silicon host.
- Added strict password-aead-v2 types and crypto helpers: well-formed Unicode/NFC policy,
  fixed-order authenticated metadata, AES-256-GCM, exact KDF-profile validation before allocation,
  generic unlock failures, identity re-derivation, fresh salt/IV/custody ID, best-effort buffer
  clearing, and a deterministic end-to-end vector.
- Added an IndexedDB active-custody store with atomic migration markers, custody-ID compare-and-swap,
  concurrent-tab exclusion, and guarded migration completion/deletion. Typecheck and 11 focused
  crypto/storage tests pass; service migration, lock lifecycle, and Settings UI remain WP2 work.
- Added an isolated WP2 custody coordinator for set-password migration, read-back verification,
  interruption recovery on the next successful unlock, and current-password-gated password changes
  with fresh salt/IV/custody ID and rollback attempts. Four coordinator tests cover successful
  migration, failed legacy cleanup with dual-copy recovery, wrong-current-password preservation,
  and existing-record conflicts. Production startup/UI wiring is intentionally still gated.
- Final validation for this WP2 foundation: typecheck, full source lint, 15 focused tests,
  `git diff --check`, and the production web build pass (only the existing bundle-size warnings).
  Production-only npm audit still reports the same 21 repository findings and does not identify
  the newly pinned `@noble/hashes` package.
- Committed that WP2 custody foundation as `fc17a1c1` (`feat(identity): add password custody
  foundation`), excluding the user-owned `AGENTS.md` change.
- Continued WP2 with production-service/startup integration: legacy custody migrates atomically on
  set, protected startup blocks on a non-dismissible unlock dialog, explicit lock clears the
  in-memory pair and reloads, and current-password-gated change rotates the v2 envelope without
  changing the SEA public identity.
- Added staged Identity & devices controls, exact no-reset warning/acknowledgement, generic unlock
  errors, forgotten-password erase guidance, Chinese copy, IndexedDB wipe coverage, 18 focused unit
  tests, and a real-browser set/reload/wrong-password/unlock/change/lock lifecycle proof. Settings
  mutation controls remain disabled in production builds pending the security review's remaining
  release conditions; startup unlock remains available for existing v2 records.
- Specified and implemented the initial WP2 lifecycle boundary: refresh/navigation/close/quit and
  explicit lock drop live service/Gun identity references; background, minimize, and idle do not
  claim automatic locking until interrupted-write platform tests exist. Extended stage1/71 to
  prove the unload handler clears the in-memory pair while preserving the encrypted v2 record for
  the next password-gated reload.
- Reconciled the set-password UI with canonical architecture section B3: it now shows the exact
  permanent-loss warning and acknowledgement, includes local length guidance, keeps Set disabled
  until valid matching input plus acknowledgement, and uses the approved “Why can't this password
  be reset?” help label instead of the prohibited “Forgot password?” wording.
- Extended the browser lifecycle proof through a wrong-current-password change failure followed by
  a successful change, and checked that both passwords stay out of localStorage, IndexedDB custody
  serialization, DOM text, console messages, and observed request URLs/bodies.
- Added custody-manager storage fault injection: a failed set read-back removes the unverified v2
  commit while preserving legacy custody, and a failed change read-back atomically restores the old
  password record. Both paths prove a known-good unlock remains after the reported failure.
- Extended the real-browser accessibility matrix so both the set-password and startup-unlock
  dialogs fit without horizontal overflow at 320 px; the cancellable dialog also restores focus,
  while startup unlock remains non-dismissible and focused on its password input.
- Product decision: Remove password deliberately returns to the existing v1 automatic-unlock
  baseline with a prominent browser-storage downgrade warning. A stronger non-extractable
  WebCrypto/native-keystore password-free v3 is deferred as a hardening TODO.
- Implemented crash-safe v2-to-v1 removal: current-password verification, exclusive IndexedDB
  transition marker, fresh v1 secret/salt/IV, full private-pair read-back verification, atomic v2
  deletion, startup completion of verified candidates, and rollback of incomplete candidates.
  Unit tests cover wrong password, concurrency, successful removal, recovery, and rollback. The
  browser journey proves warned removal and password-free reload preserve the SEA public key.
- Final validation for the expanded WP2 slice: typecheck, full source lint, 27 focused custody/UI
  tests, production web build, `git diff --check`, and all three stage1/71 Chromium scenarios pass.
  The production build retains only the existing bundle-size warnings.
- Fixed the two failures in the merged Playwright report: direct-P2P subscriptions now merge stale
  Gun snapshots monotonically so a sender-side IPFS share cannot disappear, and the mass layout
  tour captures the async Identity & devices overlay last so late refresh work cannot block its
  remaining navigation. The exact dog-talk and 33-screenshot layout-tour specs both pass.
- Started WP5 without real phones. Added the canonical transferable/device-local data inventory,
  signed checkpoint manifests, per-record/category integrity hashes, target-bound SEA encryption,
  receiver-only verified acknowledgements, and original-author/signature preservation.
- Added a resumable/idempotent importer with ordered delta heads, deterministic tombstone/version
  convergence, explicit ambiguous-conflict results, crash-window repair, and a browser custody
  store that re-encrypts imported records under the receiving installation pair. Validation: type
  check, full lint, and all 1,495 unit tests pass.
- Added the separate WP5 data-consent boundary: both installations sign the same device pair,
  mode, authorization ID, and category scope; manifests bind that ID; either side's valid later
  revocation makes the public importer reject the transfer before writing records.
- Committed the WP5 manifest/import/consent foundation as `f1497792`.
- Added the next browser-portable WP5 slice: a source-device-encrypted local outbox, monotonic
  change sequences, persisted in-flight delta bundles, offline retry, lost-ack idempotence,
  mid-transfer catch-up deltas, and sender/receiver checkpoint convergence. Five simulated
  two-installation tests cover offline, retry, catch-up, version collapse, and revocation.
