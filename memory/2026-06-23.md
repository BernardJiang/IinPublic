# 2026-06-23

- Repaired the route-mass CIDv1 assertion. A proposed server-side membership index was rolled back after it leaked process-local state across E2E specs and caused unrelated stage failures.
- Consolidated the mass specs' independent identities into isolated browser contexts inside one Chromium process, substantially reducing full-suite resource contention.
- Type-check, lint, and focused ChatroomManager unit tests passed before the rollback. The route mass spec passed in isolation; mixed saturation still needs a focused, test-only solution.
