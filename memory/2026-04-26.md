# 2026-04-26

- Updated `docs/TODO.md` to add current product backlog items for chatroom member-list scrolling, contact list/statistics, shared user detail view, Talks tab top navigation, and richer Answer tab visibility.
- Added two more backlog items in `docs/TODO.md`: unify the duplicate chatroom broadcast actions into a top navigation/status bar entry point, and add explicit E2E coverage for each new UX/bugfix item.
- Added a testing/dev-experience backlog item for reusable seeded app stages with `npm run dev:stage...` commands covering empty, one-user, two-user match/mismatch, and three-user interaction scenarios.
- Implemented the first pass of those UX/testing items: unified chatroom broadcast action into the top status bar, added Talks `All`/`IN`/`OUT` navigation with back, hardened chatroom member-list scrolling, rewired Contacts to use exchanged-talk peer summaries instead of matches-only, unified Contacts/chatroom clicks onto the same peer detail overlay, expanded Answers entries to show question/answer details, and added reusable dev stage seeds plus `dev:stage-empty`, `dev:stage-user1`, `dev:stage-user2-match`, and `dev:stage-user3-network`.
- Added targeted Playwright specs for the new UX paths, but could not execute them in this sandbox because starting the local web server failed with `EPERM` on port `8080`.
