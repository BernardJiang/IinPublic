# 2026-06-03

- Audited current implementation after `npm run test:e2e:parallel` reportedly passed. Updated `docs/TODO.md` against `docs/specs/iinpublic-technical-specification.md` §19.14.
- Key conclusion: P0 mesh delivery is passing, but P1/Phase E ownership is still incomplete. Remaining gaps include server `incomingTalksMap`/`talkResponsesMap`, global `talks/<id>/responses`, HTTP response submission authority, public incoming mirrors, full-body peer offers, and non-TechSupport conversations on server/public Gun paths.
- TODO now has a dated audit snapshot and ordered P1 action items for ownership envelopes, room announcements, zone B IN/outbox, encrypted catalog-ref offers, pair-private responses/conversations, server API scope reduction, third-party isolation E2E, and migration gates.
- Follow-up from user: make the major priority "server only connects users; all talk exchange is direct between the pair." Updated TODO to put this first, changed Playwright default so `P0_DIRECT_TALK_DELIVERY` is on unless explicitly set to `0`, updated E2E helper/docs wording to local Gun/UI as the assertion source, and retitled the direct-delivery regression. Verified with `npm run test:type` and `npm run test:e2e:p0-talks` (1 passed).
- Committed that planning/harness shift as `beb5ccb2 Prioritize pair-direct talk delivery`. Started implementation slice afterward: direct-mode broadcasts/directed sends no longer POST receiver registration, talk completion writes `pairTalkResponses/<pairId>/<talkId>/<responseId>` instead of `POST /api/talks/:id/response`, and the focused pair-direct E2E now completes the talk and asserts one pair response with zero legacy `talks/<id>/responses`. Verified with `npm run test:type` and `npm run test:e2e:p0-talks` (1 passed). This implementation slice is not committed yet.
- Committed `59df42b8 Route direct talk responses pairwise`, then finished the P2P-only E2E migration. Removed server fallback assumptions from local incoming sync, made Contacts/peer-history derive rows from local conversations and direct talk exchanges, changed E2E helpers from server conversation/history polling to local/Gun-first assertions, excluded server-snapshot triage specs from the default P2P parallel run, and updated exact chatbot memory to verify receiver-local review/confirm behavior instead of server auto-response. Verified `npm run test:type`, focused P2P batches, and final `npm run test:e2e:parallel` with default direct mode: 95 passed, 2 skipped.
