# 2026-05-09

- Optimized the slow E2E stats/demo files by replacing multi-browser UI completion loops with app-path OUT-only talk creation plus normalized stats recording.
- Added batch helper coverage in `tests/e2e/helpers/talk-demo-ui.ts` for OUT-only talk creation, direct stats recording, and in-app batch completion.
- Targeted slow-file batch now passes in 42.7s; full `CI=1 PW_WORKERS=10 npm run test:e2e -- --retries=0` passes 54 tests in 11.2m, with remaining slow files shifting to real broadcast/chatbot flows outside the original target list.
