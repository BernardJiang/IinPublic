# 2026-05-25

- Committed the verified D1 profile photo and server block-count slice as `2f2fc5f` (`Complete D1 profile photo workflow`), leaving unrelated `AGENTS.md`, `playwright.config.ts`, and `tests/e2e/staged/stage2-two-user/28-stage-zero-n2n.spec.ts` modifications untouched.
- Audited the last D1 preference-ownership item: Settings already owns all Talk Behavior and incoming-intake inputs while Me contains only answer-history controls. Added durable E2E navigation proof and reconciled the completion ledger with that existing behavior.
- Began D2 by separating persisted App language from profile and incoming-language settings. English/Chinese switching now survives reload without modifying profile language and supplies the default language for a new talk; the focused settings/navigation E2E spec passes.
- Extended D2 translation coverage through Chatrooms live/member text and Contacts list/detail/relationship/credit/block surfaces; added Chinese contact modal coverage and English singular-count regression checks.
- Extended D2 translation coverage through the Talks list and tag/flow create/respond paths, including localized row metadata, display-only language labels, and Chinese-safe persistent match notifications.
- Extended D2 coverage through populated Me answer history and the Preferences/My Talks dialogs, including localized display-only talk type badges while preserving stored type codes.
- Localized the route editor branch tree controls and default branch labels, with the Chinese traversal adding a translated child branch.
- Localized peer-detail profile, relationship, history, conversation, direct-message, and send-picker surfaces; the Chinese traversal now opens a populated peer overlay with translated language and talk-type displays.
- Localized the Settings Storage Inspector's visible headings, common statuses, permissions, policy copy, and path explanations while preserving technical diagnostic identifiers; focused Chinese navigation coverage and an in-app render check passed.
- Localized conversation overlays, previews, displayed TechSupport welcome text, and common new-conversation/talk response notices without changing the durable English TechSupport greeting stored for synchronization and bootstrap tests.
- Localized the visible Me profile, privacy, language, trend, and credit summary block; canonical TechSupport role text is translated at render time while stored seeded values remain stable.
- Localized the Me nickname/profile editor dialogs and save notices, preserving coded privacy/category values and escaping displayed stage names in the older nickname modal.
- Localized custom-room dialogs plus broadcast/travel/location room-action notices, keeping stored room payload values stable while adding Chinese dialog traversal coverage.
- Localized Talks action/validation feedback and fixed live chatroom visit/match updates that could restore English text after the language switch.
- Localized Settings distance, nickname, and photo-file validation feedback with Chinese traversal coverage for each non-mutating error path.
- Localized Contacts relationship action notices for age verification, block/unblock, and pre-match conversation guidance, with non-mutating Chinese traversal assertions.
- Localized room/conversation/direct-message feedback notices and answer-processing errors with non-mutating Chinese traversal assertions.
- Localized the populated survey analytics dashboard, CSV export notice, and follow-up survey path with Chinese assertions in its three-user E2E flow.
- Implemented the TechSupport Contacts slice: established support channels now produce a pinned built-in contact outside ordinary rankings, and Contacts plus peer detail expose local mute/unmute rather than ordinary block controls while suppressing muted support notifications.
- Closed the stale Talks language-edit TODO gap: edit saves now refresh local OUT-row language metadata, with E2E proof that an authored talk prefills its stored language and persists a changed one on reopen.
- Scoped exact chatbot memory, content-template identities, flattened answer preferences, and Me auto-use metrics by talk language so identical text no longer reuses answers across languages; legacy unscoped exact records are retained for English compatibility.
- Completed Me answer-history language display: localized badges now identify each answer row's language, and identical responses in different languages remain separate history records.
- Completed Preferences answer-memory controls: users can choose Manual, Temporary, Permanent, or Skip for exact questions, and mode edits/deletion now govern the exact-memory auto-answer path.
- Completed Me support-message exclusion: marked TechSupport welcome/support-channel records are omitted from answer history while intentionally answered TechSupport talks remain visible.
- Completed conditional-answer readability: route answer history now shows human-readable prior-question and selected-answer context instead of only route ids.
- Added a Settings default-talk-language preference: new talks follow App language until a persisted English/Chinese override is selected independently.
- Replaced Me profile free-text language codes with validated supported-language checkboxes shared with the Settings language catalog.
- Finished language-name localization by formatting creator-reply language options, filter chips, and rows through the active UI language while keeping stored codes stable.
- Completed Storage Inspector app-state coverage for TechSupport/support channels, room visit counts, and incoming/default language preferences alongside existing P2P and browser-storage diagnostics.
- Made chatroom joins idempotent for active members so reconnects do not inflate visit counters or disturb FIFO order, with audit tests for leave/rejoin and deleted-room history.
- Completed custom/business room detail metadata with localized type, headline, capacity, owner, created date, and live membership/visitor metrics.
- Stabilized regional capacity coverage by starting users in intended parent rooms and letting only the newest join resolve overflow, preventing delayed FIFO over-eviction.
- Completed room broadcast transparency with named eligible/skipped recipients, per-recipient exclusion reasons, and explicit TechSupport support-channel exclusion.
- Completed Contacts relationship-label handling so saved custom labels display, search, sort, filter, and reload without becoming generic `Custom`.
