# 2026-08-30

- Committed the pending Android 1.0.10 hardening and lazy OpenStreetMap chatroom view as `fbb1efe0`.
- Merged `origin/dev.claude` commits for cache-first production geolocation and the bundled mobile embedded-node startup path.
- Resolved the TechSupport E2E helper conflict by accepting only known signed onboarding-tip templates, not arbitrary messages beginning with `Tip:`.
- Added the missing root lockfile declaration for Claude's new direct `esbuild` development dependency so clean `npm ci` installs stay consistent.
- Post-merge verification passed: typecheck, lint, 157 unit suites / 1,666 tests, and the bundled mobile embedded-node smoke test.
- Fixed the sole saved Playwright-report failure in the first-run walkthrough spec. The shared
  `injectIdbClear` initializer had marked the walkthrough as seen even for its explicit
  `e2e_walkthrough=1` test URL; it now suppresses only ordinary E2E pages and preserves the
  dismissal flag across the opted-in spec's reload. The targeted Chromium spec passes.
