# 2026-06-04

- Completed the remaining `docs/TODO.md` P1 ownership-graph action items for the server-as-connector/direct-pair model.
- Direct-mode pair responses are SEA ciphertext under `pairTalkResponses/<pairId>/<talkId>/<responseId>`; non-TechSupport DMs are SEA ciphertext under `pairConversations/<pairId>/...`; chatroom delivery uses announcement metadata under `chatrooms/<room>/announcements/*`.
- Server/API scope reduced for direct mode: creator replies, peer relationship stats, talk history, and system snapshots no longer expose authoritative pair history from hub/server maps.
- Added third-party isolation E2E proving Alice/Bob pair response and DM ciphertext cannot be read by Tom, while one canonical talk body is reused.
- Fixed direct-mode Contacts regressions found during full-suite verification: contacts list server peer fetch now has a timeout and renders local known/conversation data; contact detail paints local profile/history first; relationship modal opens with bounded block-status wait. Added a unit regression for stalled `/peers`.
- Verification passed:
  - `npm run test:type`
  - `npx jest src/test/unit/contacts-view.test.ts --runInBand`
  - `npm run test:unit` — 29 suites, 294 tests
  - `npm run test:integration` — 5 suites, 110 passed, 1 skipped
  - Focused E2E: `14-contacts-relationship-credit`, `15a-blocking-unblock-resumes-talk-delivery`, `04-profile-edit-stage-name`
  - `npm run test:e2e:parallel` — 96 passed, 2 skipped, direct mode
  - `npm run health`
- Updated `docs/TODO.md` to mark P1 ownership graph shipped and `docs/completed.md` with full evidence.
- Follow-up flake fix after the P1 commit: pair-direct talk offers and chatroom announcements now carry the author's SEA `epub`, and pair response encryption falls back to bounded public-user retries for legacy records. This removed the `No data found for key: users/<authorId>` race in direct-mode response completion. Verification passed: `npm run test:type`, `npm run test:unit -- peer-talk-delivery` (29 suites, 294 tests), focused P2P E2E batch (11 passed), and `npm run test:e2e:parallel` (96 passed, 2 skipped).
- Moved completed P1 TODO focus/audit material into `docs/completed.md` and updated `docs/TODO.md` against the SRS (`docs/specs/iinpublic-technical-specification.md`). New active TODO focus is §19.13 / REQ-P2P-09–20: canonical PeerID, real signed envelopes, direct handshake/protocol negotiation, trust gating, schema migrations, signed upgrades, and fake-client/replay/rate defenses. Code audit noted placeholder signatures in presence/signaling/relay paths, no durable replay cache, no signed WebRTC handshake, no Verified trust level, no migration registry, and no signed upgrade flow.
- Shipped P2P-P canonical signed envelopes. Added shared canonical serialization, SHA-256-derived `peerId`, payload hashes, SEA signing/verification, and nonce replay helpers. Server presence ack, signaling, conversation relay, and discovery routes now reject unsigned/tampered/stale/wrong-peer/duplicate envelopes; browser signaling and relay polling verify before delivery; WebRTC DataChannel `ledger-state`/DM notify frames are signed and verified; direct peer talk offers carry signed sender metadata and are verified before inbox accept. Verification passed: `npm run test:type`, `npm run lint`, `npm run test:unit` (296 passed), `npm run test:integration` (110 passed, 1 skipped), `npm run build:web`, `npm run build:server`, and focused E2E signed protocol/direct-talk batch (3 passed). `docs/TODO.md` now starts with P2P-Q signed handshake/protocol negotiation.
