# 2026-04-27

- Continued the chatroom/contacts/talks/answers UX work from 2026-04-26 and fixed follow-up regressions found by the full E2E suite, including peer name fallback, contact detail compatibility, answer display recovery for ignore branches, and late conversation-list refresh when the `Me` tab is already open.
- Left two E2E failures (`tests/e2e/10-*` and `tests/e2e/12-*` per user note) for follow-up, and prepared a checkpoint commit so the current state is preserved.
