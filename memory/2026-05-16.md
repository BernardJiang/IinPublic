# 2026-05-16

- Fixed Chatrooms Return Home/detail refresh behavior: programmatic room switches now refresh the visible room title, status, and member loading state when the detail page is open.
- Fixed chatroom headcount drift: room join/leave no longer tears down global member-count subscriptions, `switchChatroom()` no longer replaces UI count callbacks with a no-op, and `getMemberCount()` now observes Gun child nodes instead of relying on a fragile parent `.once()` snapshot.
- Cleaned Talks UI states: incoming unanswered talks are visually highlighted, broadcast-enabled outgoing talks are green/highlighted, broadcast-disabled rows are greyed, and broadcast badges/controls use consistent positive “Broadcasting” semantics.
- Talks copied by default remain outgoing/broadcastable when matched; ignored/unchecked responses are saved as answered history instead of copied outgoing talks.
- Removed redundant tag Ignore checkbox; tag response now uses one match checkbox and Submit, where unchecked means ignored.
- Removed redundant Edit buttons from Talks/answer-history rows; clicking the row opens the edit/detail flow while action buttons remain separate.
- Validation: `npm run test:type`, `git diff --check`, focused E2E set (`05-talks-edit`, `07-tags-checkbox`, `08-super-user-copy-talk`, `18-travel-mode-single-room`, `talks-matching/13-tag-reopen-mismatch-then-match`) passed. Re-ran `08-super-user-copy-talk` after owned copied-talk payload fix; passed.
- User emphasized that every behavior change needs E2E coverage and reported three broken existing specs from the full suite: `00-broadcast-abort-clear-all`, `13-me-filters-credit`, and `talks-matching/11-mismatch-no-match`.
- Added `tests/e2e/00l-chatroom-talks-ui-regressions.spec.ts` to cover Return Home/detail refresh, live chatroom headcount updates, incoming new row highlighting, answered/copy-to-OUT behavior, broadcast enabled/disabled row styles, removed edit buttons, ignored/no-copy behavior, and old-talk row click handling.
- Updated `createSimpleFlowTalk()` E2E helper with an optional `sendToChatroom: false` flag so regression specs can create talks without accidental auto-broadcast races while preserving existing default behavior.
- Validation after E2E additions: new `00l-chatroom-talks-ui-regressions` passed (3 tests); the three user-reported failing specs passed together; selector-affected focused sweep passed again (5 tests); `npm run test:type` and `git diff --check` passed.
- Fixed first Chatrooms screen cold-start headcount bug reported with screenshots: member-count subscriptions now seed from child-node observation before emitting, so the list hydrates existing room counts without needing to click into room detail first.
- Added E2E coverage in `00l-chatroom-talks-ui-regressions.spec.ts` where Tom/Jerry/Bob enter Global and Bob's first list screen must show Global headcount 3 before opening detail; validation passed with `npm run test:type`, `git diff --check`, and the full `00l` regression spec (4 tests).
- Replaced Talks broadcast checkbox with a deterministic `Broadcast On` / `Broadcast Off` toggle button after user reported checkbox only toggled once; removed duplicate broadcast status badge from Talks rows and My Talks modal. Updated E2E coverage to toggle off then on and assert no broadcast status badge remains. Validation passed: `npm run test:type`, `git diff --check`, and focused E2E (`00l-chatroom-talks-ui-regressions`, `08-super-user-copy-talk`) passed 5 tests.
- User reported full suite now had one remaining failure: `tests/e2e/24-profile-privacy-visibility.spec.ts`. The spec passed alone, so hardened it for full-suite timing by waiting for the known-person API entry and viewer-filtered profile payloads before opening the UI detail panel; also widened the Global member-list wait. Validation passed: `npm run test:type`, `git diff --check`, standalone spec passed, and combined focused E2E (`24-profile-privacy-visibility`, `00l-chatroom-talks-ui-regressions`, `08-super-user-copy-talk`) passed 6 tests.
- Fixed `npm run health` integration failure in `src/test/integration/services.test.ts`: UserService now reads public profile mirrors via `getOptional()`, so the tests were updated to mock/expect `gunService.getOptional('user-public-profile/user123', 500)` instead of stale `gunService.get()` calls. Validation passed with focused integration and full `npm run health`.
