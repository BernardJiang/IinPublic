# 2026-08-24

- Pulled `origin/dev.codex` (already current) and merged `origin/dev.claude` into `dev.codex` as
  `28e92eca`, preserving the unrelated local `AGENTS.md` change.
- Refreshed the UI god-object plan with current metrics, explicit contracts, a ratcheting size
  guardrail, characterization-first extraction rules, and measured cluster ordering.
- Added a separate measurement-first React/cross-platform performance plan. React DOM is a bounded
  future pilot; React Native requires its own product decision and is not treated as a presumed
  performance cure.
- Completed UIManager decomposition cluster #1: route model logic moved to
  `route-editor-model.ts`, DOM/event ownership moved to `route-editor-controller.ts`, and
  `ui-manager.ts` dropped from 11,793 to 11,197 lines behind stable delegation.
- Fixed the stage2/21c reputation-vouch threshold regression: sender suppression now records only
  recipient-accepted ACKs, so an intake-rejected adult offer remains retryable after verification.
  Focused Playwright passed twice and the transport unit test distinguishes rejected from accepted
  recipient results.
- Canonical `test:all` run `run-20260824-181749-67574` is fully green: type, lint, 145 Jest suites /
  1,590 tests, and all 12 Playwright blobs passed in 16m36s. Cluster #1's gate is closed.
