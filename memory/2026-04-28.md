# 2026-04-28

- Fixed an order-dependent E2E leak that could make `tests/e2e/talks-matching/12-two-responders-partial-match.spec.ts` show `2 matches` instead of `1` in single-worker suite runs.
- Replaced `UIManager.setTalkStats()` merge semantics with snapshot replacement so old talk IDs do not keep inflating the status-bar match count.
- Extended `/api/test/clear-database` to also clear the server's in-memory talk response stats caches (`talkResponsesMap` and secondary indexes), not just the Gun graph and incoming-talk map.
- Verified with `PW_WORKERS=1 npm run test:e2e -- tests/e2e/10-message-unread-badge.spec.ts tests/e2e/talks-matching/12-two-responders-partial-match.spec.ts` — both passed in sequence.
- Expanded the active TODO backlog with a new pass focused on Answers metadata, Me-tab intake filters and credit visibility, and richer Contacts relationship management.
- Added `TalkIntakeFilters` and richer `KnownPerson` metadata, then wired private persistence and optimistic UI updates so the Me and Contacts surfaces share one model.
- Reworked the Answers tab to distinguish tags from normal questions, show checked/unchecked vs chosen answers, expose route context hash/path metadata, and include time/location/answer-count metadata on each grouped entry.
- Added Me-tab controls for talk intake filters and a read-only Credit section with a visibility toggle, plus Contacts-side relationship editing with nickname, rating, notes, and peer credit summary.
- Added focused unit coverage for answer-card modeling and intake filtering, and verified browser regressions with `PW_WORKERS=1 npm run test:e2e -- tests/e2e/06-contacts-tab.spec.ts tests/e2e/talks-matching/02-two-talks-status-answers.spec.ts` — both passed.
- Fixed a regression in `01-login-single-user-headcount`: the new Me-tab Credit section assumed `reputation.starRating` was always present and crashed re-login on older user records. The UI now falls back safely for missing reputation fields, and `PW_WORKERS=1 npm run test:e2e -- tests/e2e/01-login-single-user-headcount.spec.ts` passed.
- Fixed the stale Answers-tab E2E expectation in `tests/e2e/12-ux-contacts-talks-answers.spec.ts` after the redesigned answer cards changed `1 question` to `1 item` plus `answered N time(s)` metadata.
- Added new Playwright coverage in `tests/e2e/13-me-filters-credit.spec.ts` and `tests/e2e/14-contacts-relationship-credit.spec.ts` for Me-tab intake filters / credit toggle persistence and the Contacts relationship dialog save flow.
- Verified `PW_WORKERS=1 npm run test:e2e -- tests/e2e/12-ux-contacts-talks-answers.spec.ts tests/e2e/talks-matching/12-two-responders-partial-match.spec.ts tests/e2e/13-me-filters-credit.spec.ts tests/e2e/14-contacts-relationship-credit.spec.ts` with `4 passed`.
