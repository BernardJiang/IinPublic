# 2026-05-24

- Closed the Phase D1 reserved-stage-name proof gap: Settings now leaves a durable inline explanation after rejected names, and staged E2E covers all protected spellings through UI rename and API creation attempts while preserving the prior name.
- Reconciled `docs/TODO.md` and `docs/completed.md` with already-shipped D1 headshot and empty-interest behavior; remaining photo work is preview/permission fallback and wider cross-surface proof.
- Verified the slice with `npm run health` and `PW_WORKERS=20 npm run test:e2e`; health passed and Playwright completed with 77 passing scenarios and 2 intentionally skipped.
- Committed the identity-guardrail slice as `0090fe6` (`Complete D1 identity guardrails`), leaving unrelated `AGENTS.md` and `playwright.config.ts` modifications untouched.
- Continued D1 headshot work: added cancelable public-photo previews, centered camera capture with denied/unsupported guidance, authoritative public-photo reload reconciliation, and the missing visible Me profile host. Focused profile-foundation E2E passes; remaining proof is a mocked successful live-camera capture plus Contacts-list display.
- Completed the D1 headshot proof: staged E2E now covers successful mocked camera capture and avatar visibility through matched Contacts as well as upload/reload/remove/fallback behavior. During verification, fixed online block-count ordering by routing browser block operations through the server API and maintaining confirmed server writes across delayed Gun visibility; the isolated reputation block/unblock scenario now passes.
- Verified the completed D1 photo and block-count slice with `npm run health && PW_WORKERS=20 npm run test:e2e`: health passed, and Playwright completed with 77 passing scenarios and 2 intentional skips.
