# 2026-04-29

- Committed the updated prioritized backlog in `docs/TODO.md` as `77c0aa9` (`Update TODO with remaining spec gaps`).
- Added `docs/roadmap/spec-gap-matrix.md`, a file-level evidence map from the technical specification to the current codebase.
- Linked `docs/TODO.md` to the new matrix so execution priority and implementation evidence stay in sync.
- Clarified product intent for travel/chatrooms: one user instance should be in only one active chatroom at a time, including travel; removed the old multi-room-membership wording from the TODO and matrix.
- Implemented the next backlog step, profile foundation: Me tab now supports editable public headshot, languages, and profile Q/A attributes; chatroom peer detail and Contacts render the public subset.
- Added a dedicated public Gun node `user-public-profile/<userId>` for profile foundation data because the shared `users/<id>` record was not reliably carrying array fields across peer/server reads.
- Added regression coverage for the new profile flow in `tests/e2e/04-profile-edit-stage-name.spec.ts` and service-layer coverage in `src/test/unit/web-user-service.test.ts` plus `src/test/integration/services.test.ts`.
- Verification on Apr 29, 2026: `npm run test:type`, `npm test -- --runInBand src/test/unit/web-user-service.test.ts src/test/integration/services.test.ts`, and `PW_WORKERS=1 npm run test:e2e -- tests/e2e/04-profile-edit-stage-name.spec.ts tests/e2e/06-contacts-tab.spec.ts tests/e2e/11-chatroom-peer-detail.spec.ts` all passed.
