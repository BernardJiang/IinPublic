# 2026-07-04

- Updated desktop packaging so `dist:win` / `desktop:dist:win` build Windows x64 by default, with `dist:win:arm64` and `dist:win:all` available for ARM64 or both architectures. Generated a fresh unsigned x64 Windows installer at `platforms/desktop/dist/IinPublic Setup 1.0.0.exe`.
