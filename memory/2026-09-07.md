# 2026-09-07

- Began implementing `docs/IinPublic_cross_platform_e2e_test_matrix_TODO.md` at Stage 1.
- Added `test:e2e:firefox`, `test:e2e:browsers`, and `test:e2e:mixed-browsers` commands.
- Expanded the platform smoke gate to verify HTTP, WebSocket, localStorage, IndexedDB, and Gun persistence across Chromium, WebKit, and Firefox; all 6 project/test combinations passed.
- Added the first simultaneous mixed-engine scenario: `chromium-alice` authors a matching Talk, `webkit-bob` answers, and both thread directions deliver; passed.
- Fixed the first Chromium-only helper assumption by avoiding CDP window resizing outside Chromium.
- Updated the matrix checklist conservatively; the full legacy suite, Firefox mixed-peer paths, three-peer topology, reconnect, and restart persistence remain open.
- Added a deterministic three-phone Android matrix (`tests/matrix/devices.json`), serial-safe sequential APK installation with per-device timeouts, clean-sandbox preflight, real-device metadata attachments, and restart-aware WebView/CDP attachment.
- Built and installed 1.0.27 on all three connected phones (Android 10, Android 10, Android 7). The matrix exposed that Android packaging did not guarantee the existing Gun radix patch; `build:embedded-mobile` now applies it before bundling, eliminating the reproduced fresh-store native crash.
- The seven-runtime run reached simultaneous presence across macOS Electron, all three phones, Chromium, WebKit, and Firefox. It then revealed uncontrolled mDNS discovery importing another LAN node's 43-talk graph, so native E2E launches now explicitly disable LAN discovery while ordinary app launches retain it. Full Talk-ring validation remains to be rerun after this isolation build.
- After installing the isolation build, the full seven-runtime physical matrix passed in 4.8 minutes: seven identities/presence records, seven authored Talks, seven broadcasts, and seven matching completions in a ring. Exact completion edges were Firefox→macOS→Android Alice→Android Bob→Android Charlie→Chromium→WebKit→Firefox.
- Added failure-time ADB diagnostic attachments and replaced two WebCrypto-only SHA-256 helpers with `@noble/hashes`, because nodejs-mobile v18 lacks `globalThis.crypto.subtle`; the portable known-answer test plus 29 related P2P tests pass.
- Added the Windows 10 x64 host `windows-test` as an opt-in remote matrix worker. Passwordless SSH works; portable Node 24.20.0 is installed under the remote user profile. The runner gates all work on SSH/OS availability, deploys the exact committed revision into an isolated directory, runs Windows Chromium/WebKit/Firefox platform smoke, and copies/merges blob reports back on the Mac. Availability-only preflight passes on BERNARDJIANGPC.
- First Windows run installed 1,377 npm packages, applied the Gun radix patch, downloaded all three Playwright engines, and built web/server successfully. It stopped before tests because the ESM server wrapper imported a CommonJS entry point whose intentional `require.main` guard prevented auto-start; the wrapper now explicitly instantiates `IinPublicServer`. PowerShell archive progress is suppressed for later runs.
- The corrected Windows run passed all six platform-smoke cases in 2.0 minutes on BERNARDJIANGPC: UI/state and HTTP/WebSocket/localStorage/IndexedDB/Gun coverage under Chromium, WebKit, and Firefox. The remote blob report copied back and merged into the Mac's Playwright HTML report. The availability gate ran before deployment, installation, builds, or tests as required.
