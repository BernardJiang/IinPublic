# 2026-04-20

- Rewrote `README.md`, `docs/guides/HOW_TO_RUN.md`, and `docs/reports/PROJECT_STATUS.md` to reflect the current TypeScript repo instead of older merged-project docs.
- Added `docs/current/README.md` and `docs/archive/README.md` to mark source-of-truth docs versus historical material.
- Tightened `.gitignore` for generated/runtime artifacts, including `coverage/` and `.claude/worktrees`.
- Updated `jest.config.js` to ignore nested workspace and artifact paths so local runs do not pick up `.claude` worktree packages.
- Validation passed with `npm run test:type` and `npm test -- --runInBand`.
- Ran `PW_WORKERS=4 npm run test:e2e`; Playwright passed with `21 passed (6.0m)`.
- Committed the first docs/tooling cleanup as `371d553 docs: align repo docs and test config`.
- Continued Phase 1 follow-up cleanup by archiving legacy example code, legacy root files, imported `.docx` docs, and removing tracked generated coverage output plus other obvious generated clutter from version control.
