# 2026-04-20

- Rewrote `README.md`, `docs/guides/HOW_TO_RUN.md`, and `docs/reports/PROJECT_STATUS.md` to reflect the current TypeScript repo instead of older merged-project docs.
- Added `docs/current/README.md` and `docs/archive/README.md` to mark source-of-truth docs versus historical material.
- Tightened `.gitignore` for generated/runtime artifacts, including `coverage/` and `.claude/worktrees`.
- Updated `jest.config.js` to ignore nested workspace and artifact paths so local runs do not pick up `.claude` worktree packages.
- Validation passed with `npm run test:type` and `npm test -- --runInBand`.
- Ran `PW_WORKERS=4 npm run test:e2e`; Playwright passed with `21 passed (6.0m)`.
- Committed the first docs/tooling cleanup as `371d553 docs: align repo docs and test config`.
- Continued Phase 1 follow-up cleanup by archiving legacy example code, legacy root files, imported `.docx` docs, and removing tracked generated coverage output plus other obvious generated clutter from version control.
- Started Phase 2 by adding `npm run health`, aligning CI around that validation path, replacing the stale ESLint config with one that matches installed dependencies, and fixing the resulting lint blockers in `src/shared/reputation.ts` and `src/test/setup.ts`.
- Extracted server route slices out of `src/server/index.ts` into `src/server/routes/system-routes.ts`, `user-routes.ts`, `chatroom-routes.ts`, `stats-routes.ts`, and `talk-routes.ts`, leaving only the more coupled talk response/broadcast paths in `index.ts`.
- Validation for the Phase 2 checkpoint passed with `npm run health`, full Playwright `PW_WORKERS=4 npm run test:e2e` (`21 passed (6.0m)`), and targeted current-tree Playwright for `tests/e2e/talks-matching/10-stats-four-types.spec.ts` (`1 passed (1.7m)`).
- Extracted the remaining talk delivery endpoints from `src/server/index.ts` into `src/server/routes/talk-delivery-routes.ts`, covering talk receipt, broadcast receiver registration, incoming-talk listing, and talk response handling.
- Validation for the talk-delivery extraction passed with `npm run health` and another full Playwright run `PW_WORKERS=4 npm run test:e2e` (`21 passed (6.0m)`).
