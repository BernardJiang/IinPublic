# 2026-08-29

- Implemented a Tree/Map toggle for the Chatrooms view using lazily loaded Leaflet with OpenStreetMap tiles.
- Map markers represent public built-in chatrooms and aggregate counts only; the map code does not accept or expose precise user coordinates. Custom rooms without public coordinates remain in Tree view.
- Added English/Chinese accessibility text, responsive mobile layout, an OpenStreetMap attribution link, complete leaf-room coordinate coverage, and staged E2E coverage for toggling and opening a marker.
- Verification: typecheck, lint, production web build, all 1,660 unit tests, and all 5 chatroom-navigation Chromium tests passed. Browser-control visual surface was unavailable; responsive behavior was verified in Playwright at 360×800.
