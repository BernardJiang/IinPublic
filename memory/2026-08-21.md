# 2026-08-21

- Began implementing `docs/IinPublic Identity & Key Architecture TODO.md`.
- Completed WP0 vocabulary/semantics reconciliation via
  `docs/architecture/identity-v1-semantics.md`: SEA public key is the Identity ID; v1 links are
  direct mutual `LINK_IDENTITY` assertions only; no transitive cluster, implicit sync, merged
  authorship/data, password recovery, or identity-continuity authority.
- Started WP1: Settings now opens an **Identity & devices** surface with identity fingerprint and
  copy-ID action, protection status, privacy-minimized renameable local device metadata, and direct
  linked identities. English and Chinese copy added.
- Fixed pairing UI to place `currentUser.pub` (SEA public key) in the payload instead of the
  internal `currentUserId` UUID.
- Added local metadata/fingerprint unit coverage and expanded stage1/71 E2E. Typecheck, lint, focused
  unit tests, stage1/71, and stage1/77 pass. First E2E attempt hit occupied port 3001; reran with
  `E2E_PORT_OFFSET` successfully.
- Completed WP1 accessibility and responsive work: focus trapping/restoration, Escape handling,
  screen-reader labels, nested-dialog handling, and 320 px layout coverage. Typecheck, lint,
  focused unit tests, stage1/71, and stage1/77 pass.
- Drafted `docs/security/local-identity-password-custody-design.md` as the WP2 security gate. It
  documents current v1 localStorage limitations, a proposed Argon2id + AES-256-GCM v2 envelope,
  authenticated metadata, IndexedDB transactional replacement, migration rollback, lifecycle,
  recovery non-goals, and the required security review/test matrix. Custody code remains gated.
- Wired `WebIdentityLinkService` into `app.ts`. Entered codes now publish real SEA-signed
  one-sided attestations and show **Waiting for approval**, not a verified link. Fixed an existing
  graph-path mismatch: writes used a user-public namespace while reads looked at the shared
  `identity-links` root. Shared-root write/revocation unit tests and stage1/71 browser coverage pass.
