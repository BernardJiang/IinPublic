# 2026-09-19

- Finished the current software-only cross-platform matrix slice on `dev.codex`.
- Browser password-free custody v3 is now wired into production startup/password removal with
  copy→verify→delete migration, fail-closed conflict handling, erase/reset support, and persistent
  real-browser restart coverage across Chromium, WebKit, and Firefox.
- Added a production Firefox geolocation permission harness for granted and denied behavior.
- Added macOS Electron close/relaunch identity and reconnect coverage, Android logical-device
  selection, and a typed central matrix orchestrator with availability preflights and combined
  JSON/Markdown reporting.
- Stage 8 now has deterministic latency and first-N packet-loss coverage plus a real relay-process
  restart test. The restart assertion retries a versioned application write until the replacement
  relay receives it, avoiding reliance on Gun's private peer-event plumbing.
- Targeted validation passed: typecheck, lint, 45 focused unit tests, custody restart in all three
  browser engines, Firefox production permissions, identity/password lifecycle, device erase,
  Electron restart, matrix dry runs, and relay restart recovery.
- Reconciled the canonical task queue after merging `origin/dev.claude`: reduced
  `docs/TODO.md` from 2,983 lines/237 completed checkboxes to 25 stable ordered `OPEN-nn` issues,
  assigned IDs to 10 deferred decisions, removed stale duplicate checkboxes, and archived the
  completed browser/native/cross-host/matrix/custody work in `docs/completed.md`.
- Implemented the executable Tier 1 Mac-mini backlog. OPEN-02 now has packaged Electron↔Chromium
  durable chatroom-leave coverage and server DELETE synchronization; OPEN-05 has a central
  `local-scale` profile with real 8-profile survey and 10-profile flow runs. The retained matrix
  report passed in 142,144ms at
  `test-results/matrix/matrix-2026-09-20T00-26-10-913Z-48806/combined-summary.json`.
- Added safe opt-in OPEN-03/04 host harnesses for real macOS sleep/wake and loopback-port-scoped PF
  isolation. Both preflight and exit 77 without changing host state because this account lacks
  noninteractive `pmset`/`pfctl` sudo; those two physical runs remain open.
- Stabilized and completed OPEN-01: canonical `npm run test:all` run
  `run-20260919-165854-37068` passed all 12 E2E phases plus type/lint/Jest in 22m15s at a six-worker
  light ceiling. Fixes included current custody-v3 assertions, CryptoKey-safe offline context
  reuse, graph-relay device handoff, a durable route-conversation wait, and session-lifetime
  protection of mutually confirmed deals from delayed retraction frames.
- Fresh packaged macOS durable-leave verification passed 1/1 in 16.8s on alternate ports 8980/3901
  because a legitimate sibling-worktree native test owned the default 9078 port. The DMG icon hook
  warned that the Xcode license is unaccepted, but packaging continued and the `.app` test passed.
