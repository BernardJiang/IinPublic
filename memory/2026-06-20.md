# 2026-06-20

- Moved stale completed GUI sections G0–G6 and verified P1 chatroom hierarchy bootstrap from `docs/TODO.md` into `docs/completed.md`.
- Fixed flattened Me answer rows to retain flow/route context while displaying one question per row; updated its unit test contract.
- Began P3: location changes now publish privacy-safe hierarchical chatroom affinity and hierarchical room selection replaces opaque grid-room selection.
- `npm run health` is green: 718 unit tests, 79 integration tests (1 skipped), type check, lint, and web/server builds.
- P2 remains blocked on a real stable TechSupport SEA service key/public key; current root identity is virtual and has no key configured for secure signing/cross-checking.
- Repaired the five `test:all` E2E regressions after the hierarchy/answered-history refactor: location expectation now targets `new-york-state`; answered-IN tests explicitly disable the independent auto-copy setting; and the three-user matrix does the same before asserting its answered-IN ledger.
