# 2026-07-06

- Updated `docs/TODO.md` to capture the current open work from the native-app/dev-topology thread: canonical pairwise conversations, matched-talk manual continuations, TechSupport-as-bootstrap semantics, `dev:multi` topology smoke, user-visible presence cleanup, native app E2E participation, LAN browser smoke, and production compatibility.
- Moved already-covered test areas to `docs/completed.md`: peer-detail direct-message receive regression, `dev:multi` stale Global headcount reset, search/filter coverage, retired talk-delivery route proof, mobile multi-user coverage, offline mailbox TTL + SIGKILL recovery, and existing language/localization/filter coverage.
- Current actual remaining language gap is TechSupport bot behavior itself: multilingual greeting/reply semantics are not the same as the generic UI and intake-language tests.
- Implemented room-membership TTL cleanup: shared TTL env overrides, `ChatroomManager` stale active-member pruning, public room-count republish, browser membership heartbeat, `PATCH /api/chatrooms/:id/members/:userId`, unit/integration tests, and E2E `42-stale-room-membership-prune.spec.ts`. TODO now keeps only the exact OS-level SIGKILL/native crash room-count proof open.
- Added the exact OS-level crash room-count proof in `tests/e2e/staged/stage2-two-user/43-crash-room-membership-prune.spec.ts`: Bob runs in a persistent Chromium profile, gets SIGKILLed via the crash helper, expired room membership is pruned by `/members`, and Alice sees Global drop back to TechSupport + Alice. Moved that TODO item to completed.
- Added a deterministic `dev:multi` topology smoke. `scripts/dev-techsupport-bootstrap.js` seeds the TechSupport root before ordinary browser profiles navigate, `launch-browsers.js` uses that bootstrap during the clean reset flow, and `scripts/smoke-dev-multi.js` verifies exactly TechSupport plus three browser users in Global. Verified with `DEV_MULTI_SMOKE_WEB_PORT=3361 npm run smoke:dev:multi`.
- Implemented the first-user support-flow contract: an empty-network ordinary browser login no longer becomes the TechSupport root, support greetings use deterministic `support_welcome_<userId>` ids, and the single-user E2E now asserts ordinary-user identity, TechSupport + user headcount, one support conversation, and one welcome after reload. Verified with `npm run test:type`, `npx jest src/test/unit/techsupport.test.ts --runInBand`, and `E2E_PORT_OFFSET=320 E2E_GUN_MEMORY_ONLY=1 DISABLE_HMR=true PW_WORKERS=1 npx playwright test tests/e2e/staged/stage1-single-user/01-login-single-user-headcount.spec.ts`.
- Finished the E2E helper TechSupport semantics audit: added `tests/e2e/helpers/techsupport-contract.ts` and wired root-vs-ordinary assertions into canonical, talks-matching, mobile, and super-user bootstraps; updated staged docs to distinguish TechSupport bootstrap root from ordinary stage users. Verified with `npm run test:type` and `E2E_PORT_OFFSET=340 E2E_GUN_MEMORY_ONLY=1 DISABLE_HMR=true PW_WORKERS=1 npx playwright test tests/e2e/staged/stage2-two-user/34-contacts-filter-name.spec.ts`.
- Implemented multilingual TechSupport bot-path coverage for supported UI languages: support-channel user messages now receive a localized TechSupport auto-reply, Chinese reply covered in `00k-techsupport-contact-mute.spec.ts`, and unsupported profile languages still fall back to English via `uiLanguageFromProfile`. Verified with `npm run test:type`, `npx jest src/test/unit/ui-translations.test.ts --runInBand`, and `E2E_PORT_OFFSET=360 E2E_GUN_MEMORY_ONLY=1 DISABLE_HMR=true PW_WORKERS=1 npx playwright test tests/e2e/staged/stage2-two-user/00k-techsupport-contact-mute.spec.ts`.
- Defined and enforced the TechSupport root/bootstrap contract in `docs/design/techsupport-bootstrap-contract.md`; chatroom member rows now label TechSupport with `data-support-contact="true"` and built-in support copy, matching the Contacts contract while status-bar ordinary counts continue excluding TechSupport. Verified with `npm run test:type`, `npx jest src/test/unit/techsupport.test.ts --runInBand`, and `E2E_PORT_OFFSET=360 E2E_GUN_MEMORY_ONLY=1 DISABLE_HMR=true PW_WORKERS=1 npx playwright test tests/e2e/staged/stage2-two-user/00k-techsupport-contact-mute.spec.ts`.
- Implemented canonical ordinary pair conversations: `WebConversationService.createConversation()` now returns `conv_pair_<sorted user ids>` independent of talk id, preserves related matched-talk ids as Gun-safe JSON metadata, and direct peer-detail sends reuse the local conversation map key. Retraction/withdrawal checks now match related talk ids. Verified with `npm run test:type` and `E2E_PORT_OFFSET=400 E2E_GUN_MEMORY_ONLY=1 DISABLE_HMR=true PW_WORKERS=1 npx playwright test tests/e2e/staged/stage2-two-user/00e-chatroom-peer-detail.spec.ts --grep "peer detail direct message|peer detail shows talk history"` (2 passed).
- Closed the canonical pair reload-history gap. Root causes were: message ids could be observed before full Gun nodes were readable; older async collections could overwrite a longer rendered history with a shorter one; pair-root reads could hang instead of falling back to the legacy conversation root; mailbox-drained DMs used the receiver id as `otherUserId`, creating a self-pair path; and E2E readiness could open a conversation before `loadConversation` handlers were bound. Fixes landed in `GunMessageStore`, mailbox ingestion, `IinPublicApp.initialized`, and `waitForAppReady`. Verified with `npm run test:type` and `E2E_PORT_OFFSET=417 E2E_GUN_MEMORY_ONLY=1 DISABLE_HMR=true PW_WORKERS=1 npx playwright test tests/e2e/staged/stage2-two-user/31-messaging-history-order.spec.ts` (1 passed).
- Extended `31-messaging-history-order.spec.ts` to cover the remaining local conversation-depth slice: after the real 12-message Gun-backed ordered exchange, it renders a 54-row conversation snapshot on both pages for >50-message scroll coverage and asserts edit/delete controls are absent from the current conversation UI. Updated `docs/TODO.md`, `docs/completed.md`, and the spec companion `.md`. Verified with `npm run test:type` and `E2E_PORT_OFFSET=433 E2E_GUN_MEMORY_ONLY=1 DISABLE_HMR=true PW_WORKERS=1 npx playwright test tests/e2e/staged/stage2-two-user/31-messaging-history-order.spec.ts` (1 passed).
- Closed the conversation reconnect E2E-depth item by extending `37-hard-crash-recovery.spec.ts`: after B is SIGKILLed, relaunched, and receives A's two offline DMs, B now sends a post-reconnect reply back to A in the same canonical pair conversation. This found a real bug: mailbox DM drain used the sender epub to decrypt but did not cache it by sender id, so B could fail to encrypt a reply to A after recovery. `IinPublicApp.drainMailboxOnce()` now caches the wrapper sender epub for `conversation-message-v1`. Verified with `npm run test:type` and `E2E_PORT_OFFSET=442 E2E_GUN_MEMORY_ONLY=1 DISABLE_HMR=true PW_WORKERS=1 npx playwright test tests/e2e/staged/stage2-two-user/37-hard-crash-recovery.spec.ts` (1 passed).
