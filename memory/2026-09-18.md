# 2026-09-18

- Completed `docs/TODO.md` Priority 4 on branch `dev.codex` (not committed yet).
- §EE: self-authored non-location built-ins now persist as structured `typedValue` fields in the
  flat Me-tab AnswerRecord/history store. `typedPreferenceState` remains a resolver index, gains
  source talk/question provenance, and is written only after create/update succeeds.
- Typed preference keys now include normalized route Q→A context (excluding Pair-tag ancestors),
  preventing identical typed prompts on separate route branches from overwriting each other.
- Owned-talk update handling now reuses `saveCreatedTalk`, updating My Talks, AnswerRecords, and
  typed lookup entries together. The route editor E2E verifies create, Me display, reopen, edit,
  and sibling-branch isolation.
- §KK's alleged ad-hoc/no-own-talk gap was stale after §LL: `effectiveTagContext` already reverses
  the incoming Pair-tag ancestor into precise own/counterpart tags. Added unit characterization.
- Validation: type-check, lint, full unit (225 suites / 2,293 pass, 8 skipped), full integration
  (90 pass, 1 skipped), production web build, and the updated Chromium E2E all passed.
- Local `node_modules/@noble/curves` had drifted to invalid 2.2.0 while package/lock require 1.8.1;
  `npm install --no-audit --no-fund` restored dependencies without tracked lockfile changes.
- Completed Priority 5 K7 production TechSupport key custody/rotation (also uncommitted): encrypted
  AES-256-GCM+scrypt vault, strict private-file modes, shared server/operator loader, public trust
  config, overlap-aware client verification, and guarded prepare/activate/retire CLI with
  transactional artifact re-signing. Plaintext env input is now migration-only.
- Added the production runbook and corrected the VPS guide to prefer an off-server encrypted vault.
  Current signed-identity protocol still binds one SEA pair, so v1 rotates DM and announcement
  roles together; the config preserves distinct roles for a future protocol split.
- K7 verification: typecheck/lint and web/server builds pass; 225 unit suites / 2,298 tests pass
  (8 skipped), all 6 key-tool tests pass, and 10 integration suites / 90 tests pass (1 skipped).
