# 2026-06-07

## P2P mesh talk delivery implementation slice

- Converted `docs/p2p-mesh-talk-delivery-plan.md` into active `docs/TODO.md` action items and moved shipped hub migration rows to `docs/completed.md`.
- Added `P2P_MESH_TALKS` runtime flag, webpack exposure, shared mesh protocol types, DataChannel `mesh` frames, and `PeerMeshService`.
- Mesh service now supports signed origin verification, seen-set dedupe, TTL forwarding, room neighbor sessions, `mesh-ping`, `talk-announce`, `talk-body-request`, `talk-body`, and `talk-response`.
- App integration: mesh flag routes broadcast/direct talk sends over mesh, receivers ingest mesh talk bodies through the existing local incoming-index/intake path, and pair-private responses route over mesh with local author-side match/conversation handling.
- Verification passed: typecheck, unit suite, integration suite, lint, server build, web build, legacy P0 direct-talk E2E, and in-app browser smoke on `localhost:3001` with `P2P_MESH_TALKS=1`.
- Remaining work is mostly proof/deletion: browser mesh E2E, offline mailbox, local-only contacts/history, and final star talk route removal.
