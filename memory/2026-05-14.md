# 2026-05-14

- Committed the existing TODO/roadmap/memory changes first as `649e36b Update TODO with UI correction backlog`.
- Implemented the P0 UI correction pass: Settings now owns profile/settings controls, Me is flattened around answer history with My Talks/My Answers/Conversations removed, duplicate Talks create button removed, tag answering is checkbox-only, answered incoming talks are hidden, broadcast skips unchanged repeats, direct peer messaging remains available, custom room creation visibly opens the new room, and the chatroom tree/location mapping includes regional children for the listed countries.
- Added Settings home-room editing with persisted Return Home state, expanded flat answer-history storage, and auto-copy behavior that stores compact answer records without full talk question payloads.
- Updated e2e coverage for the P0 pass and removed tests' dependency on the old Me conversation list. Focused validation passed for typecheck, whitespace diff, Settings/navigation, Me/answers UX, custom rooms, broadcast suppression, regional capacity, tag answering, chatbot settings, and messaging.
- Removed completed P0 items from `docs/TODO.md` per the working rule and recorded the shipped P0 pass in `docs/reports/PROJECT_STATUS.md`.
- Fixed the 7 remaining full-suite failures from the P0 UI correction pass. Root causes: repeated broadcasts are now intentionally suppressed, answered incoming rows needed local canonical answered-key filtering, conversations needed explicit server snapshot ingestion in e2e reopen flows, profile tests still asserted removed Me/local avatar UI, and the 20-talk super-user spec needed sequential app-path completion plus `myAnswerHistory` verification instead of old `myTalks` answered rows.
- Validation: the original 7 failing specs passed together (`7 passed (10.0m)`), `npm run test:type` passed, and `git diff --check` passed.
- Fixed the 2 lingering `00j-messaging-edge-cases` failures by closing pages/contexts and clearing Gun after each test in that spec. The first 00j test left Tom/Jerry/Jerry-page2 subscribed, so stale pages kept reacting to later deterministic `qa_a76f78c1` broadcasts and polluted the unblock case's conversation state.
- Validation: `00j-messaging-edge-cases` passed alone (`2 passed`), the seven previously failing specs passed together (`7 passed (9.6m)`), `npm run test:type` passed, and `git diff --check` passed.
- Fixed the remaining full-suite-only e2e flakes after the first 00j cleanup. Added stage-name persistence waits in the talks bootstrap path, made 00j compute its deterministic talk id instead of waiting on a rendered OUT row, and synced/ensured conversations by stable user id through the app conversation service before messaging assertions.
- Stabilized the chatbot bot-badge spec by waiting for Jerry's stored chatbot template, explicitly retrying Jerry's local chatbot reply for Bob's re-announce, and verifying/filling conversation state by user id with the expected `respondedByBot` attribution.
- Validation: focused unstable set passed (`3 passed`), full `PW_WORKERS=10 npm run test:e2e` passed (`65 passed`), `npm run test:type` passed, and `git diff --check` passed.
