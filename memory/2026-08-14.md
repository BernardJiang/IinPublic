# 2026-08-14

- Prepared IinPublic for a single-origin production deployment: Node/Express now serves the built web client, API, Socket.IO, Gun relay, health endpoint, and app-download manifest.
- Corrected Docker production output/CMD, added a safe Docker build context, removed production exposure of the repository root, and added explicit Gun worker-asset serving.
- Added configurable hosted installer URLs plus macOS/Windows/Linux/Android/iOS manifest support and platform detection in the web UI.
- Added `render.yaml` and `docs/guides/DEPLOY_PRODUCTION.md`; Render chosen for the first deployment because it supports long-lived Node web services, Docker, WebSockets, custom domains, and managed TLS.
- Production build, focused tests, lint, local HTTP/WebSocket/security checks passed. Local production server left running at http://localhost:8080.
- DNS inspection: `www.iinpublic.com` still CNAMEs to `ghs.googlehosted.com`; authoritative nameservers are the old Google Domains `ns-cloud-c*.googledomains.com` set now associated with Squarespace Domains.
- Publication is waiting for the user to sign in/create a Render account in the visible browser. Do not publish the currently staged desktop installers/debug APK to normal users until signed release artifacts are available.
- User required HTTPS for all user-facing cases. Added a fail-closed TLS mode: direct local HTTPS requires certificates; hosted plaintext is allowed only behind an explicitly declared HTTPS-terminating proxy; plain HTTP remains only for isolated E2E tests and native on-device loopback. Local production now runs at https://localhost:8080 and rejects http:// connections.
