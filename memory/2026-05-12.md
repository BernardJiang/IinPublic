# 2026-05-12

- Completed the `docs/TODO.md` implementation pass for IinPublic: exact chatbot memory UI/history, server-side exact memory integration coverage, mobile/offline/location E2E coverage, and documentation/status refreshes.
- Fixed location refresh behavior so the app and UI state update after `switchChatroom()` selects a regional chatroom.
- Validation passed: targeted unit/type/build/integration checks, new E2E specs, and full `PW_WORKERS=10 npm run test:e2e` with 58 passed tests.
- Reorganized active docs around `docs/specs/iinpublic-technical-specification.md`: added `docs/README.md`, archived stale phase/coverage/import-source docs, rewrote `docs/TODO.md` as a forward statistics backlog, and added `docs/roadmap/statistics-expansion.md`.
- Completed the statistics expansion backlog in `docs/TODO.md`: added shared stats dashboard schemas/aggregators, server stats dashboard/time-series/cross-question/chatroom/peer endpoints, Gun-backed stats hydration, a web Statistics tab, docs updates, and unit/integration/E2E coverage.
- Reordered long-running E2E specs into the first 10-worker wave using `00d` through `00j` names, then verified the full suite with `PW_WORKERS=10 npm run test:e2e`: 59 passed in 6.6 minutes.
- Completed the UI/navigation overhaul: bottom nav is Chatrooms/Contacts/Talks/Me/Settings, Answers is merged into Me with answer filters, Stats nav is removed in favor of contextual stats strips, Settings exposes user-controlled filters/toggles, Chatrooms/Contacts/Talks/Me each have status/action bars, Return Home uses the smallest location room, and capacity overflow creates blurred regional rooms. Validation passed: `PW_WORKERS=10 npm run test:e2e` with 61 passed in 6.9 minutes, plus targeted UI/return-home E2E and `npm run test:type`.
- Refined the Chatrooms top area in commit `5f08a24`: removed generated user stage name and duplicate stats strip from the view body, moved room/user/match status into the top status bar, removed the Talks OUT hint, and kept Back/New Room/Return Home/Broadcast as compact action-row controls. Validation passed: `npm run test:type`, `git diff --check`, and targeted E2E specs including `tests/e2e/13-chatroom-scroll-and-broadcast-bar.spec.ts`.
- Follow-up Chatrooms header refinement: removed the visible top-left `Chatrooms` title, moved the chatroom status text into the first-row header beside the user avatar, and removed the generic blue styling for level-0 rooms so only `.current-room` is highlighted. Validation passed: `npm run test:type`, `git diff --check`, targeted E2E specs `00-ui-navigation-settings`, `04-profile-edit-stage-name`, `07-tags-checkbox`, and a Playwright localhost smoke check with no Oops screen.
- Extended the same header/action/content layout to Contacts, Talks, Me, and Settings: all tab status text now lives in the shared top header beside the user avatar, each tab starts with its action bar as the second row, and body-local status bars were removed. Validation passed: `npm run test:type`, `git diff --check`, `tests/e2e/00-ui-navigation-settings.spec.ts`, and `tests/e2e/07-tags-checkbox.spec.ts`.
