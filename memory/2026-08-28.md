# 2026-08-28

## Post-merge Playwright regression repair

- Merged `origin/dev.claude` into `dev.codex` as `a863d697`; the merged first-run
  walkthrough initially blocked 256 Playwright scenarios by intercepting app-shell clicks.
- Automated E2E bundles now suppress only the automatic walkthrough unless the test URL
  opts in with `e2e_walkthrough=1`; a dedicated E2E spec covers first run, persistence,
  and Settings replay so production behavior remains tested.
- Updated TechSupport bootstrap assertions for the signed onboarding tips that now follow
  the signed welcome, and registered the new Help & Tour Settings section in shared E2E navigation.
- Tests that need a newly-authored Talk now wait for the authoritative OUT projection instead
  of assuming a fixed 200 ms commit, preserving the no-ghost-row production guarantee.
- Fixed a real mesh dedup race: direct and relayed copies arriving during async signature
  verification are now coalesced without permanently accepting an invalid frame id.
- Final `npm run test:all` passed every phase in 17m6s: typecheck, lint, Jest, light, stage5,
  mesh batch/isolated, find-similar, cross-browser, isolated, heavy-staged, and mass.

## Version 1.0.10 Android deployment

- Synchronized root, lockfile, desktop, Android, iOS, and SBOM metadata to 1.0.10; Android
  uses versionCode 1000010. Built the current debug APK successfully from `effc6e9a` and
  verified its package metadata and SHA-256.
- FRD-L04 completed an in-place `adb install -r`; C10 and VOG-L29 completed the proven 128 KiB
  chunked path with full device-side APK SHA-256 verification before `pm install -r`. Application
  data was preserved. `dumpsys package` confirms all three devices at 1.0.10 / versionCode 1000010.
- VOG-L29 passed the automated real-device boot test. FRD-L04's Playwright attachment timed out,
  but direct process and Chromium logs prove the 1.0.10 embedded SPA booted and actively synced.
  C10 rebooted to recover its flaky ADB shell; its rerun was blocked by the post-reboot keyguard
  (`showing=true`, input restricted). It needs one manual unlock before rerunning the boot test.
