# 2026-04-30

- Continued the TODO execution after profile foundation by implementing server-enforced intake moderation on the delivery path.
- Moved the pure intake-filter evaluation into `src/shared/talk-intake-filters.ts` so both the web UI and server routes use the same language/grammar/dirty-words/type/time/distance logic.
- Added a server-readable `user-talk-filters/<userId>` Gun mirror; the Me-tab filter editor still updates owner-private state, but now also writes the public enforcement mirror for delivery-time checks.
- Enforced receiver-side filtering in `POST /api/talks/:id/received` and `POST /api/talks/:id/register-receivers-for-broadcast`; filtered talks are skipped before they ever enter the server-backed incoming-talk inbox.
- Added adult-talk gating to the same delivery path: adult talks are rejected for receivers who are not age-verified.
- Regression coverage added/updated:
  - `src/test/unit/web-user-service.test.ts`
  - `src/test/integration/services.test.ts`
  - `src/test/integration/talk-loop.test.ts`
  - `tests/e2e/13-me-filters-credit.spec.ts`
- Verification on Apr 30, 2026:
  - `npm run test:type`
  - `npm test -- --runInBand src/test/unit/talk-intake-filters.test.ts src/test/unit/web-user-service.test.ts src/test/integration/services.test.ts src/test/integration/talk-loop.test.ts`
  - `PW_WORKERS=1 npm run test:e2e -- tests/e2e/13-me-filters-credit.spec.ts`
  - `PW_WORKERS=1 npm run test:e2e -- tests/e2e/13-me-filters-credit.spec.ts tests/e2e/06-contacts-tab.spec.ts`
