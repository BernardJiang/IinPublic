# 2026-05-22

- Fixed four stale E2E headcount expectations after TechSupport became the always-seeded root user. The affected specs now always include TechSupport in Global counts, independent of `E2E_STAGE_PIPELINE`.
- Verification passed: `npm run test:type`, targeted four-spec Playwright run, and full `npm run health && PW_WORKERS=20 npm run test:e2e` with `77 passed`, `2 skipped`.
- Fixed the parallel-load exact chatbot memory regression by making server reads ignore Gun metadata-only nodes and fall back to a per-server exact-memory cache. The cache is cleared on E2E database clear/import so staged snapshots do not carry stale memory. Verified with the isolated exact-memory spec and the full `npm run health && PW_WORKERS=20 npm run test:e2e` command.
