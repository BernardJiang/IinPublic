# 2026-05-21

- Added `TechSupport Root Network Role` to `docs/TODO.md`: scoped `TechSupport` as the singleton/root P2P network support identity, global-room anchor, greeter for new users, and per-user support-channel bootstrap contact.
- The TODO now includes an implementation action list plus a tab-by-tab verification checklist covering Chatrooms, Contacts, Talks, conversation/peer overlays, Me, Settings, and cross-session/multi-user behavior.
- Expanded `docs/TODO.md` with a `Feature Completion Backlog by Tab`: full UI localization after profile language selection, clearer multi-language incoming talk filter UX, default talk language and language badges/filtering, chatroom lifetime visitor counters, TechSupport-specific contact/block behavior, Me-tab answer memory mode gaps, Settings/storage-inspector completeness, and hidden Statistics dashboard scope.
- Added TODO scope for E2E stage pipeline migration: consolidate all single-user specs into TechSupport Stage 0, save Stage 0 only after TechSupport traverses single-user coverage, load that baseline for later stages, verify ordinary users receive TechSupport greeting/support channel before normal tests, and update headcount/broadcast/contact expectations for permanent TechSupport presence.
- Committed the TechSupport plan as `8942468 Document TechSupport root roadmap`.
- Implemented a first TechSupport root slice: shared `TechSupport` constants/reserved-name checks, server/web user creation support for canonical root id, first-run web bootstrap that creates TechSupport before ordinary users, server-backed root detection for cold clients, Global seeding, per-user support welcome conversation, and durable welcome-message state.
- Added chatroom lifetime `visitCount` and `uniqueVisitorCount` counters in web/server chatroom services and surfaced them in Chatrooms rows/detail.
- Reworked Settings incoming talk language filter from native multi-select to checkbox/chip controls, added active count, defaulted new Talk editor language from the user's profile language, persisted talk language, and displayed language badges on IN/OUT talk rows.
- Updated staged E2E: Stage 0 now logs in TechSupport, traverses core single-user tabs, saves `stage0` after verification, and ordinary bootstrap helpers verify a TechSupport support greeting/channel. Stage pipeline helpers now preserve the TechSupport baseline instead of clearing to an empty graph in pipeline mode.
- Verification run: `npm run test:type` passed; unit subset and integration subset passed before final E2E tweaks; `npm run build:web` passed; Stage 0 pipeline passed; targeted Stage 1 legacy language filter passed; targeted Stage 2 headcount with TechSupport baseline passed. A broad accidental Stage 2 dependency run exposed one stale language-filter test (fixed) and one survey timeout before it was stopped.
